{
  "name": "pulse",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev:web": "pnpm --filter pulse-web run dev",
    "dev:fastify": "pnpm --filter pulse-server run dev"
  }
}
